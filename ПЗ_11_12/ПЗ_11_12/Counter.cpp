#include <iostream>
/*
class shipment {
public:
    double volume;
    double weight;
    double cost() {
        return this->volume / 4 + this->weight / 7;
    }
};
bool greater(shipment a, shipment b) {
    return a.cost() > b.cost();
}
int main() {
    shipment a{}, b{};
    std::cin >> a.volume >> a.weight;
    std::cin >> b.volume >> b.weight;
    if (greater(a, b))
        std::cout << 1;
    else if (greater(b, a))
        std::cout << -1;
    else
        std::cout << 0;
} */